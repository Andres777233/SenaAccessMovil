# AGENTS.md — Movilvistas ("SenaAccess MÓVIL")

App Android (Kotlin + Jetpack Compose, estilo glass verde) de SennAccess. Ruta: `/home/andres/Escritorio/VistasAndroidDef1-main./VistasAndroidDef1-main` (la carpeta padre termina en punto; citar siempre entre comillas).
No es repo git local; el código está en `git@github.com:Andres777233/VistasAndroidDef1.git` (rama `main`). Para push: clonar → superponer código → commitear → pushear.

## Comandos y entorno
- Compilar: `./gradlew assembleDebug` → APK en `app/build/outputs/apk/debug/app-debug.apk`.
- Instalar (moto g86): `/home/andres/android-sdk/platform-tools/adb install -r app/build/outputs/apk/debug/app-debug.apk`. El `adb reverse tcp:8000` lo aplica solo el watcher (≤3 s tras conectar USB).
- Backend: servicio systemd de usuario `sennaccess` (`systemctl --user status|restart sennaccess`); el watcher lo relanza si muere.
- Android Studio Flatpak: si el sync falla con "initialization script ... does not exist", ejecutar `./gradlew --stop` antes.
- Credenciales (BD Railway + local, ver seeder del WEB): `admin@sena.edu.co` con `12345678`; instructores: `juan.pablo@sena.edu.co` (`12345678`), `alejandro/gustavo/raul@sena.edu.co` (`123456`); aprendices: `andres.vargas/laura.medina@sena.edu.co` (`12345678`), `katherin/sebastian/camilo@sena.edu.co` (`123456`). NO existen `instructor@` ni `aprendiz@sena.edu.co`.

## Convenciones
- API: `RetrofitClient.conServicio{}` habla SOLO con Railway (HTTPS, sin fallback local); timeout conexión 10 s / lectura 60 s.
- Patrón `CargaUiState` (Loading/Success/Error); mocks SOLO en `data/mock/MockData.kt` y solo si `SessionManager.token == null`.
- Paquetes: dashboards en `com.example.sennaccess.Aprendiz` (comparten StatCard/TableContainer), ViewModels en `ui`, repos en `data`.
- Comentarios nuevos solo `//` en línea propia (sin asteriscos/emojis/cajas); cabecera 2-4 líneas `//` por archivo.
- Biometría LOCAL (`data/HuellaCredentialStore.kt`, Keystore AES256-GCM); el WebAuthn del backend quedó sin uso desde el móvil.

## Retomar trabajo (IMPORTANTE)
- Este proyecto = **"SenaAccess MÓVIL"**.
- Palabra clave: **"continuemos con Sena access movil"** (o "continuemos"/"retomemos"/"seguimos"). En ese caso NO leas archivos extra y NO re-explores el repo con grep/glob — el estado completo y todo el historial están AQUÍ MISMO (sección `## Historial`, entradas más recientes al final).

## Autoguardar historial (OBLIGATORIO)
Al terminar cada tarea/sesión, agrega al FINAL de `## Historial` una entrada fechada `- YYYY-MM-DD — <qué cambiaste y por qué>`: MUY concreta, directa y clara (1-2 líneas máximo). Nunca borres ni reescribas entradas anteriores.

## Historial
- 2026-08-22 — Paridad con el backend + refresco: quité de la app REPORTES/ASIGNACIONES/MENSAJE, arreglé dock EQUIPOS del admin (abre inventario); refetch automático al cambiar/volver de pestaña. APK compilado PENDIENTE de instalar en el moto g86.
- 2026-08-22 — USB automático permanente: servicio systemd user `sennaccess` + watcher cada 3 s (levanta artisan serve y aplica adb reverse solo con dispositivo autorizado); enable-linger activo; `iniciar-servidor.sh` quedó solo-estado.
- 2026-08-22 — Avatar/foto perfil: `ui/AvatarPerfil.kt` (Coil, rutas relativas vía `SessionManager.fotoUrl()`), CAMBIAR FOTO multipart (`updateMyProfileWithPhoto`, _method=PUT+image); admin solo muestra avatar.
- 2026-08-20 — Biometría LOCAL (reemplaza WebAuthn/passkeys): huella descifra credenciales del dispositivo (Keystore AES256-GCM, SharedPreferences `huella_store`); auto-sanitiza si el backend rechaza; WebAuthn queda sin uso desde el móvil.
- 2026-08-20 — FIX crash splash: themes.xml pasó a padre `Theme.AppCompat.Light.NoActionBar` (MainActivity extiende AppCompatActivity por BiometricPrompt).
- 2026-08-12 — Todos los GETs conectados a la API real (patrón CargaUiState, mocks solo en MockData.kt); logout best-effort crea Salida; historiales con badge ENTRADA/SALIDA; RegistrarEquipoView con accesorios dinámicos.
- 2026-08-12 — Push a GitHub VistasAndroidDef1 (estrategia clonar→superponer→push único "VersionBackend"); convención comentarios solo `//` línea propia.
- 2026-08-11 — Base técnica: Retrofit doble ruta USB/WiFi con fallback 3s; dashboards comparten StatCard/TableContainer; navbar/menú vidrio verde; RegisterScreen con diálogo "Solicitud enviada"; fix DirectoryLock de Android Studio Flatpak.
- 2026-08-24 — Preparado para Railway: RetrofitClient añade BASE_URL_REMOTE (HTTPS) como 3er fallback USB->WiFi->Railway; backend WEB con railway.json (nixpacks + migrate --seed) y .gitignore que protege .env. Ambos repos SUBIDOS a GitHub (SenaAccessWeb / SenaAccessMovil). Falta: deploy Railway + poner dominio real en BASE_URL_REMOTE y recompilar APK.
- 2026-08-24 — Fix build Railway (WEB): nixpacks.toml (PHP 8.3 + ext gmp/sodium/intl/bcmath + COMPOSER_ALLOW_SUPERUSER) para el fallo de package:discover como root; .gitkeep en storage/bootstrap (carpetas viajan en el repo, contenido ignorado); releaseCommand con mkdir/chmod + migrate --seed. Empujado a SenaAccessWeb (7d03601).
- 2026-08-24 — Fix "valid cache path": composer.json añade pre-autoload-dump con mkdir -p de storage/bootstrap antes de package:discover; probado localmente borrando views/ y corriendo dump-autoload (exit 0). Dominio Railway OK (senaaccessweb-production.up.railway.app); BASE_URL_REMOTE actualizado y APK compilado PENDIENTE instalar en moto g86 (no estaba conectado).
- 2026-08-24 — APK instalado en moto g86 y login probado contra Railway: funciona. Distribución QR: repo QR-MOVIL actualizado (SenaAccess.apk apuntando a Railway, página v1.0) + QR en ~/Escritorio/QR-SenaAccess-v1.0.png.
- 2026-08-24 — FIX login 500 con PC apagada: la BD "MySQL" de Railway estaba rota (solo placeholders, password root perdida, sin DATABASE_URL en el web). Creé BD nueva "MySQL-DK1Y" (railway add), inyecté en SenaAccessWeb DATABASE_URL+APP_KEY+APP_ENV+APP_DEBUG+APP_URL+LOG_CHANNEL=stderr, migré+sembré vía `railway ssh` al contenedor web; login verificado 200 contra Railway. RetrofitClient ahora Railway primario (REMOTE->USB->WIFI); APK compilado PENDIENTE instalar.
- 2026-08-25 — Pantalla Asignaciones (aprendiz↔instructor): Repository + ViewModel + AsignacionesView (listar/crear/eliminar con dropdowns de aprendiz/instructor/jornada); dock admin ahora tiene 6 tabs (se añadió "Asignar"). Verificación contra Railway: POST/GET/DELETE `/admin/aprendiz-instructores` funciona; equipo-a-usuario y novedades ya estaban cableados y verificados. APK compilado PENDIENTE instalar.
- 2026-08-25 — Quité la feature Asignaciones (aprendiz↔instructor) a petición del usuario (también se quitó del WEB): borré AsignacionesView/AsignacionRepository, revertí el dock a 5 tabs, limpié ViewModel/Mock/ApiService/Models. Además FIX: la pestaña EQUIPOS del admin estaba vacía porque `irATab("EQUIPOS")` no abría subScreen; ahora abre `AdminScreen.EQUIPOS` y el admin puede registrar/ver equipos con dueño (instructor/aprendiz). APK compilado PENDIENTE instalar (celular no conectado).
- 2026-08-25 — Perfil con foto en los 3 roles + UI más llamativa: nuevos `ui/PerfilUI.kt` (PerfilHeader con avatar 116dp anillo/glow + chip rol, FilaDato con ícono); `EditarPerfilView` gana `mostrarFichaPrograma` para el admin (oculta ficha/programa); ApiService/UsuarioRepository hacen opcionales ficha/programa en el multipart para que el admin suba foto; AdminDashboard añade sub-pantalla EDITAR_PERFIL. PerfilAprendizView/PerfilInstructorView/PerfilContent usan el nuevo diseño. APK compilado PENDIENTE instalar (celular no conectado).
- 2026-08-25 — FIX foto perfil (500) + ficha solo para Aprendiz: en el backend WEB `user_coursenumber`/`user_program` pasan a `required` SOLO para Aprendiz (updateMyProfile/createUser/updateUser) y Railway recibió CLOUDINARY_URL/CLOUDINARY_UPLOAD_PRESET (sin esto subir foto daba 500). Móvil: `EditarPerfilView` con `mostrarFichaPrograma=false` también para el instructor (no envía ficha/programa), `UpdateProfileRequest` con ficha/programa nullable, catch de error muestra código+detalle del servidor, y las vistas de perfil de admin/instructor muestran ficha solo si >0. Deploy Railway 34c08d55 verificado (login 200). APK compilado PENDIENTE instalar (celular se desconectó).
- 2026-08-25 — FIX foto no se guarda/error al cambiarla: `EditarPerfil.kt` ahora comprime la imagen elegida con `comprimirImagen()` (muestreo + escala a máx 1024px, JPEG calidad 80) y siempre sube como image/jpeg; evita el 422 por fotos >2MB o HEIC. CAUSA RAÍZ en Railway: la migración nullable de ficha/programa estaba PENDING ahí y el UPDATE con ficha null daba 500 tras subir bien a Cloudinary; aplicada vía `railway ssh` (columnas Null=YES) y subida verificada end-to-end contra Railway (HTTP 200, URL Cloudinary persistida). Backend también subió límite a max:5120 (push 6c137c0). APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-25 — Red y errores (decisión: SOLO Railway): `RetrofitClient` sin fallback USB/WiFi (era la causa probable del "Error 500" del dashboard: el celular caía al backend local roto), timeouts 10s/60s; `detalleHttp()` en CargaUiState/EditarPerfil muestra código+mensaje real del servidor (adiós al genérico "No se pudo conectar"); `comprimirImagen` con takePersistableUriPermission. APK instalado en moto g86; pendiente confirmar en el dispositivo que dashboard carga sin 500 y la foto guarda.
- 2026-08-25 — FIX "No se pudo leer la imagen": el `?: return null` de comprimirImagen se disparaba SIEMPRE porque decodeStream con inJustDecodeBounds devuelve null por diseño. Reescrita: ImageDecoder (API28+, ALLOCATOR_SOFTWARE, escala a 1024px) + BitmapFactory antigua + fallback bytes crudos; mensaje local ya no dice "Fallo de conexión". APK instalado en moto g86; pendiente prueba real del usuario.
- 2026-08-25 — Recortador circular de foto de perfil (sin librerías): en EditarPerfil.kt nuevo `RecortarFotoDialog` a pantalla completa (marco 300dp + anillo verde, gestos pinza-zoom/arrastre con límites para que el círculo siempre quede cubierto); al confirmar recorta con Matrix a Bitmap cuadrado 512px JPEG q85 (`bitmapAJpeg`) que se sube por multipart. Flujo: galería → recortar → previsualiza avatar → guardar; `comprimirImagen` eliminada (reemplazada por `decodificarImagen` máx 2048px). APK compilado e instalado en moto g86 (ZY32MFMZMX).
- 2026-08-25 — QR v1.1: repo QR-MOVIL actualizado (push 74aaed6 vía SSH; el clon local vive en /tmp) con el APK nuevo y página "v1.1 — actualizada 25/08/2026"; despliegue verificado en andres777233.github.io/QR-MOVIL (APK sirve 200). Nuevo QR en ~/Escritorio/QR-SenaAccess-v1.1.png (misma URL, verificada decodificándola).
- 2026-08-26 — Pulido v1.1: fechas legibles (ui/Formatos.kt: "25 ago 2026, 09:30", relativo "hace 5 min" en notis/novedades, hora corta en admin), pull-to-refresh en los 3 dashboards (PullToRefreshBox), estados vacíos amables (EstadoVacio en ui/EstadoUI.kt), pantalla Acerca de/versión (ui/AcercaDeView.kt, BuildConfig habilitado, versionName 1.1) accesible desde el menú de los 3 roles, y diálogo de confirmación real al borrar usuarios. APK compilado y QR-MOVIL actualizado (push 563f2bd, página v1.1 26/08/2026); pendiente instalar en moto g86.
- 2026-08-26 — Registro funcional + tipo de documento y teléfono: backend WEB añade columnas `user_documento_tipo` (CC/CE/TI/PAS) y `user_telefono` (migración + fillable + register/createUser/updateUser) y valida que el correo termine en @gmail.com, @hotmail.com o @soy.sena.edu.co (422 con mensaje claro si no). ROL resuelto: el registro público queda SOLO para aprendices (activos al instante, se corrigió el mensaje falso de "un admin validará tu cuenta"); los instructores SOLO los crea el admin desde su panel (CrearUsuario). Móvil: RegisterScreen con chips de tipo doc + teléfono y diálogo de éxito honesto; CrearUsuario/ActualizarUsuario con los campos nuevos; Models (RegisterRequest/UserRequest/User/UsuarioApi). WEB React: Register.jsx y Admin.jsx con select tipo doc + teléfono. Verificado contra Railway (gmail 201, soy.sena 201, outlook 422, login admin/instructor 200). Deploy e57dbb69→03030bc. APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-26 — Pulido registro: el tipo de documento pasa de chips a dropdown con significado ("CC: Cédula de Ciudadanía", "CE: Cédula de Extranjería", "TI: Tarjeta de Identidad", "PAS: Pasaporte") en RegisterScreen/CrearUsuario/ActualizarUsuario (mismo formato en Register.jsx/Admin.jsx) y el label del correo queda "Correo Electrónico" (sin "Institucional"). APK instalado en moto g86 (ZY32MFMZMX); WEB push f261336.
- 2026-08-26 — Flujo invitado COMPLETO (QR + escáner): backend WEB `registerGuest` ahora acepta `user_lastname` (push 289a345, auto-deploy Railway). Móvil: GuestForm (LoginScreen) con documento+nombre+apellidos llama `POST /register-guest`, valida con `detalleHttp` y muestra el QR de un solo uso (60 min) en `ui/QRUtils.kt` (genera Bitmap con zxing:core 3.5.3). Escáner en recepción: `admin/EscanearQrView.kt` (CameraX 1.4.2 + MLKit barcode) valida `POST /validate-guest-qr`; accesible desde menú de admin (AdminScreen.ESCANEAR_QR) e instructor (top bar). Verificado end-to-end contra Railway (guest con apellido 201, validación 1er uso OK / 2do uso "QR ya utilizado", entrada "Validación QR" en historial del admin). APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-26 — Control de permanencia "quién está dentro": backend WEB nuevo `GET /admin/presentes` (AdminController, push 68fdf5b): usuarios cuyo último ingreso es Entrada sin Salida (subquery MAX(id_ingreso)). Móvil: `Presente` en Models + `getPresentes` en ApiService/IngresoRepository, estado `presentes` en AdminDashboardViewModel, nueva `admin/Presentes.kt` (total + tarjetas con hora de entrada `horaCorta` y estancia `fechaRelativa`), pestaña 6ª "PRESENTES" en el dock del admin (ícono Groups). FCM (push) diferido. Verificado contra Railway (lista actual). APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-29 — Dashboard Aprendiz/Instructor enriquecido (sin quitar pestañas): `ResumenView` y `InstructorResumenView` ahora con banner bienvenida (avatar+ficha/programa), estado DENTRO/FUERA, actividad reciente (3) y equipos recientes (2) con "Ver todo" → dock, e instructor además novedades recientes (2) + acción Escanear QR; `TarjetaNovedad` pública para reuso; nuevos helpers `BienvenidaDashboardCard`/`SeccionDashboardTitulo`/`EstadoAccesoDashboardCard`/`TarjetaActividadDashboard`/`TarjetaEquipoDashboard` en `AprendizDashboard.kt:581`. BUILD SUCCESSFUL, APK en `app/build/outputs/apk/debug/app-debug.apk` pendiente instalar.
- 2026-08-29 — Limpieza dashboard (a petición): quitadas StatCards "Mis Ingresos/Equipos" y "Usuarios/Ingresos" y sección "TUS EQUIPOS" de `ResumenView` e `InstructorResumenView` (`AprendizDashboard.kt:327`, `InstructorDashboard.kt:349`); el Inicio queda solo con banner bienvenida + estado DENTRO/FUERA + actividad reciente (instructor además novedades + botón Escanear QR). BUILD SUCCESSFUL (44M), `adb install -r` OK en moto g86 (ZY32MFMZMX).
- 2026-08-29 — Autocompletado de correos + menú foto/lápiz: `SessionManager.kt:58` añade `guardarCorreoUsado`/`obtenerCorreosUsados` (SharedPreferences `login_prefs`, máx 5, sin duplicados); `LoginScreen.kt:92` guarda el correo al `LoginUiState.Success` y `NormalLoginForm:585` muestra `ExposedDropdownMenuBox` con `sugerenciasCorreo` filtradas (`menuAnchor`); `AprendizDashboard.kt:209` y `InstructorDashboard.kt:213` añaden `onEditarPerfil` y el header del menú pasa de `Icon(Person)` a `AvatarPerfil(fotoPath=SessionManager.userPhoto, 48.dp)` con el lápiz como botón circular verde (22dp) `clickable { onEditarPerfil ?: onPerfil }` → `EDITAR_PERFIL`. BUILD SUCCESSFUL, APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-29 — FIX modo claro (contenedores blancos debajo de texto): causa `ThemeText` blanco fijo sobre vidrio blanco → texto invisible; corregido `LoginScreen:518/590/858`, `RegisterScreen:449`, `PasswordReset:138/314`, `PasswordRecovery:165/384`, `QRUtils:91` a `colors.textPrimary/Secondary`; `GlassComponents:120` ahora distingue claro/oscuro (luminance) con base 0.92, highlight 0.55/0.18, sombra suave y borde definido; `Color:120` paleta clara más contrastada (bg 0xF0F2F5, tablas blancas/filas alternas, bordes 0.13); `GlassDock:75` sombra/borde adaptativos; `MainActivity:59` y `SplashScreen:100` usan `appColors.background`. BUILD SUCCESSFUL, APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-29 — Pulido splash + limpieza instructor: `SplashScreen.kt:81` acorta delays (650→450, 320→280, glow 500→420), glow más intenso en claro (0.72/0.28 vs 0.55/0.18) y añade `v${BuildConfig.VERSION_NAME}` con `LocalAppColors.textSecondary`; `InstructorDashboard.kt:37/52` quita imports `EscanearQrView`/`TarjetaNovedad`, `InstructorTopBar:213` elimina `onEscanearQr` y el DropdownMenuItem "Escanear QR invitado", `InstructorResumenView:358` elimina params `novedadesEstado/onVerNovedades/onEscanearQr` y borra secciones "MIS NOVEDADES" (L447) y "Control de invitados/ESCANEAR QR" (L478). BUILD SUCCESSFUL, APK instalado en moto g86 (ZY32MFMZMX).
- 2026-09-01 — Módulo presencia física + FSM jornada (v1.2): desacopla login de registro de presencia; 3 capas zero-trust — TOTP QR rotativo 30s (instructor proyecta, `jornada/GenerarQrAulaView.kt` + `TotpUtils.kt` liviano HMAC-SHA1), GPS con anti-spoofing (`UbicacionProveedor.kt` + `isMock`/`isFromMockProvider`, sin Play Services) y BSSID (`WifiProveedor.kt`); FSM `REGISTRADO→EN_AULA⇄EN_DESCANSO→FINALIZADO` con salida anticipada bloqueada (403) y token de permiso auditable (`JornadaViewModel.kt` + `AutorizarSalidaView.kt`); nuevo `AprendizDock` tab Jornada (`MarcarPresenciaView.kt`), instructor con QR Aula+Permisos en dock y admin en menú; contrato `ApiService.kt:259`/`ModelsJornada.kt` (NTP, ventanas por ambiente, cron 18:00). BUILD SUCCESSFUL v1.2 (44M), pendiente instalar.
- 2026-09-02 — Módulo ambientes + el QR de invitado eliminado (v1.3): eliminado `LoginScreen GuestForm`/`QRUtils QrInvitadoView`/`EscanearQrView`/rutas `register-guest`/`validate-guest-qr` (WEB `AuthController`+`api.php`); backend migraciones `ambientes`+`ambiente_instructor`+`ambiente_aprendiz` (hora_inicio/fin, totp_secret auto, capacidad/ubicación/jornada), modelo `Ambiente.php` y `AmbienteController.php`+`JornadaController.php` (TOTP 30s, `mis-ambientes`); móvil `AmbienteRepository.kt`/`AmbientesAdminView.kt` (admin crea ambientes multi-instructor, CCyS/Ciudad Jardín) + `MisAmbientesView/AmbienteDetalleView` (instructor ve sus salones, agrega/quita aprendices, accesos directos QR/Permiso); `InstructorDashboard` dock reestructurado 5 tabs. BUILD SUCCESSFUL v1.3 (44M) instalado en moto g86; verificado local: admin crea ambiente 101/202, instructor Juan Pablo en 2 ambientes, QR `SENA-JORNADA:1:283330:30` válido.
- 2026-09-02 — Fix 408 ambientes + excusas con PIN (v1.4): causa 408 era deploy desactualizado (Railway en 68fdf5b sin modulo); commit 25d10f9 deploy + railway ssh migrate crea ambientes; verificado POST/PUT/GET/DELETE /admin/ambientes y /mis-ambientes/{id}/aprendices contra Railway (101 CCyS y 202 Ciudad Jardin con Juan Pablo). Excusas: backend migracion excusas + Excusa.php/ExcusaController.php con PIN 4 digitos (60 min, un solo uso) — instructor POST /instructor/excusas -> PIN, admin POST /excusas/validar registra Salida en ingresos; movil ModelsExcusa.kt/ExcusaRepository.kt + excusas/CrearExcusaView.kt (instructor elige ambiente/aprendiz->PIN copiable) + excusas/ValidarExcusaView.kt (admin valida) + excusas/MisExcusasView.kt (aprendiz) integrados en InstructorDashboard (AMBIENTES->CrearExcusa), AdminDashboard (menu Validar excusa), AprendizDashboard (dock EXCUSAS). Verificado Railway: PIN 1232 usado, 5416/1992 pendientes. BUILD SUCCESSFUL v1.4 (45M) pendiente instalar (moto g86 no conectado).
- 2026-09-02 — Limpieza v1.4: quitada tab JORNADA del aprendiz (`AprendizDashboard.kt:77` quita `JornadaViewModel`/`MarcarPresenciaView` y `GlassDockItem JORNADA`; dock queda Inicio/Excusas/Historial/Equipos) y menú "Autorizar salida" del admin (`AdminLayout.kt:204`); el flujo real de salida es Validar excusa PIN (admin) y Crear excusa PIN (instructor). BUILD SUCCESSFUL, APK reinstalado en moto g86 (ZY32MFMZMX) y QR-MOVIL v1.4 actualizado (push ea1721a, 45M).
- 2026-09-06 — v1.5 búsqueda/filtros + presencia en vivo: `Presentes.kt` reescrito con búsqueda por nombre, chips de filtro por rol (Todos/Aprendices/Instructores), auto-refresh silencioso cada 30s, reloj local que recalcula la estancia cada 60s, orden por entrada descendente y exportar CSV (FileProvider); `AdminEquipos.kt` gana buscador local por propietario/tipo/marca/serial con estado vacío "Sin coincidencias". BUILD SUCCESSFUL (45M), APK pendiente instalar en moto g86.
- 2026-09-06 — v1.6 rediseño "Personas en el centro" por ambientes: reescrita `Presentes.kt` para mostrar tarjetas por ambiente con ficha deducida, contadores dentro/total, barra de progreso, lista expandible de miembros con estado DENTRO/FUERA, buscador por nombre/ficha, auto-refresh y tick de estancia; eliminados QR aula y Acerca de (borrados archivos y referencias en admin, instructor y aprendiz). BUILD SUCCESSFUL (45M), instalado en moto g86 (ZY32MFMZMX).
- 2026-09-06 — Pulido Presentes del admin (v1.7): buscador ahora solo "Buscar por Ficha" (filtra por número de ficha, ya no por nombre/ambiente); tarjeta de ambiente deja de mostrar "Ficha X" deducida y al expandir cada persona muestra su rol+ficha propios, y si está DENTRO "Entró: fecha y hora" o si está FUERA "Última entrada: fecha y hora" (deducida del historial `/admin/ingresos` vía `resumen`); eliminados los auto-refresh de 30s y el tick de 60s (solo pull-to-refresh manual). BUILD SUCCESSFUL, APK pendiente instalar.
- 2026-09-06 — Rediseño vista Ambientes del instructor: `AmbienteDetalleView.kt` con cabecera destacada (ícono, nombre, chips de ubicación/jornada/capacidad, conteo de estudiantes), acciones reordenadas (primario "Agregar aprendiz" y secundario "Permiso de salida"), lista de estudiantes con inicial en círculo, chip de ficha y correo, estado vacío con mensaje amable. BUILD SUCCESSFUL, APK pendiente instalar.
- 2026-09-09 — Limpieza módulo jornada muerto: borrados `jornada/*` (ViewModel/Repository/Views/Totp/GPS/Wifi), `ModelsJornada.kt`, `AcercaDeView.kt`, bloque `/jornada/*` en ApiService y `AUTORIZAR_SALIDA` colgado; BUILD SUCCESSFUL instalado en moto g86.
- 2026-09-09 — Diseño alternable original/renovado (solo visual): `DesignMode`+`DesignModeStore`, paletas renovadas, glass/dock condicionales y opción "Diseño" en los 3 menús hamburguesa. BUILD SUCCESSFUL, APK instalado en moto g86 (ZY32MFMZMX).
- 2026-09-09 — Endurecimiento de seguridad: códigos de recuperacion guardados como hash bcrypt (antes texto plano, columna ampliada a 255), cada "olvide contrasena" invalida tokens previos, throttling en login (5/min) y forgot/reset (3/min y 10/min), verificacion de correo (enlace firmado 60 min + reenvio), errores JSON unificados via Handler, servicio central de notificaciones, comando tokens:clean (cada hora) y app: campo email_verified_at en User/UsuarioApi/SessionManager + reenvio de verificacion en ApiService/AuthRepository.
- 2026-09-09 — FIX envio de correo en produccion: Railway usaba SMTP Mailgun inalcanzable + key Resend invalida; configurado Gmail SMTP (MAIL_MAILER=smtp, smtp.gmail.com, app password) y blindado con try-catch el envio en forgotPassword/resendVerification/verificacion del registro para que el fallo de mail nunca cuelgue la peticion. PasswordRecoveryScreen: indicador ENVIANDO... y boton deshabilitado mientras se envia. APK recompilado pendiente de instalar (celular desconectado).
- 2026-09-09 — FIX definitivo correo: Railway bloquea TODO SMTP saliente (Mailgun/Gmail/Brevo 465 y 587, timeout); unica via es HTTPS. Resend API funcional con key NUEVA valida (onboarding@resend.dev entrega solo a volverasandres7@gmail.com, correo de la cuenta Resend; para enviar al correo de cualquiera hace falta dominio verificado SPF/DKIM en resend.com/domains). MAIL_MAILER=resend, forgot-password responde 200 en ~0,8s. Prueba end-to-end: recuperar cuenta con volverasandres7@gmail.com (ya registrada en SenaAccess).
- 2026-09-09 — CORREO A CUALQUIER USUARIO: Railway bloquea SMTP saliente (Mailgun/Gmail/Brevo 465 y 587); transporte HTTPS propio BrevoTransport.php (Symfony+Graph) integrado con MAIL_MAILER=brevo, remitente verificado mandarinakachivache@gmail.com (visible como SenaAccess). Brevo plan gratis 300/dia, IP auth desactivada para poder enviar desde Railway (API v3 exige remitente verificado exacto y rechaza html/text vacios con 400). forgot-password 200 en ~1s a volverasandres7@gmail.com y a cualquiera; commits a8b8e60/d64f542/f416e6d. Mobile: sin cambios (APK con ENVIANDO... ya instalado).
- 2026-09-09 — Logo en el correo de recuperacion: cabecera pasa a blanca con el logo SenaAccessLogo.jpeg embebido en base64 (data URI) porque Brevo NO soporta imagenes embebidas por CID en transaccionales; log/assets: public/email/SenaAccessLogo.jpeg, vista recovery_code.blade.php, commit cf0304d. Mobile sin cambios.
- 2026-09-09 — Logo del correo por URL (no base64): Gmail no carga data URI en transaccionales; recovery_code.blade.php usa <img src={{ url("email/SenaAccessLogo.jpeg") }}> servido por Railway en public/ (200 image/jpeg). La "s" junto al remitente en Gmail es el avatar de Google del correo remitente (mandarinakachivache@gmail.com), no algo del email. commit f948d99.
- 2026-09-09 — Logo del remitente REVERSADO del email: el logo NO va en el mensaje (se revertio recovery_code.blade.php al diseno original verde, commit 5f8a292); el logo debe ser el AVATAR de Gmail que se ve al lado del remitente = foto de perfil de Google de la cuenta remitente mandarinakachivache@gmail.com (la sube el usuario manualmente en myaccount.google.com).
- 2026-09-10 — Compatibilidad total + Docker (v1.8 build): dedupe dependencias en `libs.versions.toml` (icons-extended 1.7.8, coil 2.6.0, profileinstaller 1.3.1); quitados CameraX/MLKit y passkeys/`androidx.credentials` (código muerto: PasskeyRepository, webauthn en ApiService, Models, LoginViewModel.onPasskeySuccess); release con R8+shrink (APK 45→4.2M, debug 21M); `gradle.properties` con parallel/caching/config-cache/nonTransitiveRClass/fullMode (quitado jetifier deprecated); RetrofitClient loggea BODY solo en DEBUG; BiometricAuth añade WEAK+DEVICE_CREDENTIAL (API 30+, PIN/patrón de respaldo); Manifest: networkSecurityConfig HTTPS, sin CAMERA, windowSoftInputMode=adjustResize + configChanges; dockerizado con Dockerfile (temurin17+SDK36)+.dockerignore+docker-compose → imagen `sennaccess-movil:latest`, APK idénticos al build local (md5 490e2b90) en `artifacts/apk/`; usermod andres→grupo docker y daemon habilitado. Comprobado assembleDebug+Release local y en contenedor. Instalación en moto g86 PENDIENTE (no conectado al finalizar).
- 2026-09-11 — Merge v1.8 (GitHub/otra máquina) + verificación de correo local: rama `v1.8-merge` (commit e9c0939) unifica el push 2d63d9a (limpieza passkeys/CameraX muertos, R8 4.2M, Docker, biometría WEAK, HTTPS estricto) con el trabajo local pendiente de correo (resendVerification, email_verified_at en login, ENVIANDO… en recuperación); conflictos resueltos conservando lo mejor de cada lado. BUILD SUCCESSFUL (v1.8+), APK en `app/build/outputs/apk/debug/app-debug.apk` sin instalar.
- 2026-09-11 — Verificación en dos pasos (2FA) "¿Eres tú? in-app + código al correo (Brevo)": LoginResponse/modelos + 7 endpoints en ApiService/TwoFactorRepository (`data/TwoFactorRepository.kt`); LoginScreen detecta `two_factor_required` y no guarda sesión, muestra `ui/verificacion2fa/Verificacion2FaScreen.kt` (código 6 dígitos + polling 4 s del reto); sub-pantalla `Configuracion2FaView.kt` cableada en los 3 perfiles (botón "VERIFICACIÓN EN DOS PASOS" con estados ACTIVADA/INACTIVA, desactivar pide contraseña) y `Dashboards2FaPendientes()` ("¿Eres tú?" con IP/hora, Sí/No aprobar `2fa/aprobar`, polling 8 s) añadido en los 3 dashboards; PIN de excusas ahora 15 min (`excusas/CrearExcusaView.kt`, `MisExcusasView.kt` y backend `ExcusaController` addMinutes(15)). BUILD SUCCESSFUL, APK sin instalar; requiere deploy del backend 2FA a Railway para funcionar en vivo.
- 2026-09-11 — FIX "No se pudo conectar al servidor" en el celular (`SSLHandshakeException: Pin verification failed`): el `res/xml/network_security_config.xml` traía del merge v1.8 un `<pin-set>` con pin corrupto (`SHA-256 AAA...==`) que nunca coincide con el certificado de Railway; retirado el pin-set del domain-config production y quitado `certificates src="user"` del base-config (queda solo HTTPS forzado + trust-anchors de sistema). Backend verificado sano (login 200 en 0.65s, 2FA desplegado). APK reinstalado en moto g86.
- 2026-09-11 — Ajustes UI (encargos del usuario) + 2FA verificado EN VIVO: icono de recarga de "Mis excusas" movido junto al título (antes al extremo derecho); opción "Diseño: original/renovado" eliminada de los 3 menús hamburguesa (Aprendiz, Instructor, AdminLayout) y limpieza de imports muertos (DesignMode/DesignModeStore/LocalDesignMode); con estos + el fix de `network_security_config.xml` (sesión previa) se compiló BUILD SUCCESSFUL. Backend 2FA ya funcional en Railway (ver historial del WEB): flujo `login→two_factor_required→pendientes(¿Eres tú?)→aprobar→estado entrega token` probado de punta a punta, reto dejó de expirar al instante tras fix de timezone (Carbon::now UTC), y se desactivó el 2FA de la cuenta de prueba andres.vargas@sena.edu.co. APK con los 3 arreglos pendiente de instalar (celular desconectado al cerrar).
- 2026-09-12 — FIX 4 bugs huella+2FA (v1.5 QR): crash huella por CryptoObject+DEVICE_CREDENTIAL (ahora solo BIOMETRIC_STRONG con crypto, `activityDe()` anti-cast null, sin `!!`, `onAuthenticationFailed`, `try/catch` en `authenticate`); rol desconocido ya no cae a admin (`MainActivity`+`LoginScreen`+`Verificacion2FaScreen` validan lista cerrada); overlay 2FA movido al final del Box y login no se compone durante el reto (antes se superponían) + reto derivado en `LaunchedEffect`; deep links `senaaccess://2fa`+`https://.../2fa` (`singleTask`, `onNewIntent`) llevan al reto desde el correo y polling tolerante cierra el overlay; `DialogoEresTu` con anti-doble-tap y diálogo de confirmación; llave Keystore se regenera (`prepararCifradoSeguro`/`borrarLlave`/`borrarTodo`) y `HuellaView` valida 2FA antes de registrar + trim consistente (también `LoginViewModel` recorta contraseña). BUILD SUCCESSFUL, instalado en moto g86 (ZY32MFMZMX), QR-MOVIL v1.5 pusheado (5268baa, APK 21M verificado 200) + `~/Escritorio/QR-SenaAccess-v1.5.png`.
- 2026-09-12 — Teclado amable + 2FA por dispositivo + splash v2.2: nuevo `ui/TecladoScroll.kt` (`campoVisible()` lleva el campo enfocado a la vista) aplicado a login/registro/recuperación/restablecer/perfil/huella/2FA/excusas/admin + `imePadding()` en sus contenedores; `MiHuellaSection` exige contraseña actual + confirmación coincidente; nuevo `data/DispositivoStore.kt` (UUID por teléfono) que viaja como `device_id` en login (`LoginRequest`/`AuthRepository`/`LoginViewModel`/`LoginScreen`+huella) para que el backend solo rete fuera del dispositivo original; `EditarPerfilView` con puerta 2FA al cambiar clave (diálogo de 6 dígitos → `two_factor_code` en `UpdateProfileRequest`, doble PUT si hay foto); plantilla 2FA estilo SENA con preheader del código en `/tmp/senaaccess-2fa/two_factor_code.blade.php` (pendiente aplicar en WEB); `versionName 2.2`/`versionCode 5` (splash muestra v2.2). BUILD SUCCESSFUL, instalado v2.2 en moto g86 (ZY32MFMZMX), QR-MOVIL v1.6 pusheado (7794173, APK 21M verificado 200) + `~/Escritorio/QR-SenaAccess-v1.6.png`.
- 2026-09-12 — Retoques finales v2.2: teclado con `imePadding()` en el Box externo de login/registro/recuperación/restablecer/verificación + columnas de ambientes/excusas y `campoVisible()` en diálogo crear-ambiente (el scroll ya no se queda arriba al escribir); 2FA por dispositivo real en WEB (migración `trusted_device_id`+`device_id`, login salta el reto en teléfono confiable y lo confía al aprobar por código/in-app/correo, cambio de clave en perfil exige código del correo vía caché 10min); email 2FA oscuro SENA con preheader del código (visible en bandeja sin abrir Gmail), asunto con el código y botón deep-link a la app; splash dice "Versión 2.2"; `EditarPerfil` pide el código al backend en vez de abrir el diálogo en vacío. BUILD SUCCESSFUL, instalado en moto g86 (ZY32MFMZMX). WEB pendiente push+deploy a Railway para que el teléfono (usa Railway) vea el 2FA y el correo nuevos.
- 2026-09-12 — FIX teclado en Perfil + huella sin bloqueo 2FA + deploy Railway: `imePadding()` y spacer final en los 3 perfiles (admin/Perfil.kt, PerfilAprendizView, PerfilInstructorView) para que la contraseña de la huella suba sobre el teclado; nuevo `POST /my-profile/verificar-password` en WEB (token + Hash::check, sin retos ni correos) y `HuellaView` lo usa con la sesión en vez de un login fresco (adiós al error "complétala en el login" y a los correos fantasma); push WEB 4f2dce6 + deploy y migrate en Railway verificados en vivo (login con device_id y verificar-password 200). BUILD SUCCESSFUL, instalado en moto g86 (ZY32MFMZMX).
- 2026-09-12 — Correos gemelos del proyecto (solo WEB, móvil sin cambios): `two_factor_code` y `recovery_code` con paleta exacta de la app (#07090D/#13161C/#02D914/#00E676/#FF6B6B), logo SenaAccessLogo.jpeg por URL y preheader con el código; push 0f53d6f + deploy Railway verificado en vivo (render 4808 chars).
- 2026-09-12 — Seguridad roles + outlook.com + dropdowns + back sistema (v2.25): nuevo `data/RolSeguro.kt` (normaliza aprendiz/instructor/admin|administrador, resto inválido) usado en `MainActivity`/`LoginScreen`/`Verificacion2FaScreen`/`LoginViewModel` (este limpia sesión vieja al iniciar login y rechaza rol inválido sin navegar); WEB acepta `@outlook.com` en `AuthController@register` + `AdminController@createUser/updateUser` (mensaje y pista en `Register.jsx`/`RegisterScreen` con validación cliente y `lowercase`); tipo doc pasa a `ExposedDropdownMenuBox` clicable completo en `RegisterScreen`/`CrearUsuario`/`ActualizarUsuario`; `MainActivity` con pila + `BackHandler` (retrocede en auth, dashboards ya no cierran la app); `versionName 2.25`/`versionCode 6`. BUILD SUCCESSFUL, instalado en moto g86 (ZY32MFMZMX), QR-MOVIL v2.25 (push 7de0eb6, APK 21M verificado 200 por md5) + `~/Escritorio/QR-SenaAccess-v2.25.png`. Diagnóstico caso arciniegasfk303@gmail.com→admin y huella-crash: era APK viejo en ese celular (fallos firma pre-2026-09-12); se resolvió borrando APKs antiguas, cuenta intacta. WEB pendiente push+deploy a Railway para outlook en vivo.
- 2026-09-12 — Borrado total + OK sin salir (v2.5): móvil `Usuarios.kt` muestra el error del DELETE en el diálogo (antes mudo) y `Crear/ActualizarUsuario` con botón "OK" que se queda en la pantalla (Cancelar vuelve a la lista, ya no al dashboard) + errores reales con `detalleHttp`; `versionName 2.5`/`versionCode 8`. BUILD SUCCESSFUL, instalado en moto g86, QR `~/Escritorio/QR-SenaAccess-v2.5.png` + `public/SenaAccessV2.5.apk` en WEB listos (WEB pendiente push+deploy).
- 2026-09-12 — 2FA funcional v2.3: correo solo deep-link directo (SÍ/NO abren la app, sin página web intermedia); "No soy yo" cierra el intento con cartel "Acceso denegado" y notifica al dueño con IP/dispositivo/fecha-hora Bogotá; "Sí soy yo" entra solo por polling; `DeepLink2FaStore` + `dec` en MainActivity + fecha absoluta en "¿Eres tú?". BUILD SUCCESSFUL v2.3 (code 7), instalado en moto g86, QR `~/Escritorio/QR-SenaAccess-v2.3.png` + `public/SenaAccessV2.3.apk` en WEB listos (WEB pendiente push+deploy).
- 2026-09-13 — Rediseño FASE 1 (auth) + QA aprobado: base `ui/ios/AuthComponents.kt` (AuthBackdrop, AuthLogo, ThemeToggleButton, AuthField, ErrorBox), `Buttons.kt` con PrimaryNeonButton (loading integrado) y GlowOutlinedButton píldora 28dp; reescritas Splash/Landing/Login/Register/Recovery/Reset con esos componentes (package corregido a `com.example.sennaccess`, adiós fondos remotos Coil y títulos WarningYellow, verde con `verdeMarca()` legible en claro, diálogos a 28dp, imports muertos y copias de botones/cards eliminados). QA reportó P0 de contraste en LoginScreen (corregidos a verdeMarca). BUILD SUCCESSFUL, APK en `app/build/outputs/apk/debug/app-debug.apk` PENDIENTE instalar.
- 2026-09-13 — Rediseño FASE 2 (shell + dashboards) + QA aprobado: quitado el fondo remoto pattern.png (Coil) de los 4 shells (Aprendiz/Instructor/AdminLayout/AdminDashboard); acentos `SenaGreen`→`verdeMarca()` en texto/íconos visibles en claro (dashboards, PanelPrincipal/AdminPanelResumen, EstadoAcceso/TarjetaActividad/Historial, badge INGRESADO/SALIDA con OrangeAmber unificado); toggle de tema manual → `ThemeToggleButton` en los 3 roles; `GlassDock` con acento adaptativo (luminance) e imports/doc-blocks `/** */` limpiados (solo `//`); botones "EDITAR PERFIL"/"VERIFICACIÓN EN DOS PASOS" de los 3 perfiles pasan a PrimaryNeonButton/GlowOutlinedButton (píldora 28dp). QA Fase 2: 4 P0 de contraste + P1 de botones 12dp corregidos. BUILD SUCCESSFUL, APK 22,219,011 bytes PENDIENTE instalar.
- 2026-09-13 — Rediseño FASE 3 (sub-pantallas) + QA aprobado: migrados a `verdeMarca()` todos los acentos de texto/íconos/inputs de las 26 sub-pantallas (excusas/*, ambientes/*, admin/{Usuarios,HistorialAdmin,CrearUsuario,ActualizarUsuario,AccesoAprendices,AccesoInstructores,AmbientesAdminView,Presentes,AdminEquipos,ReporteNovedades,Perfil}, ui/{PerfilUI,NotificacionesView,NovedadesView,HuellaView,AvatarPerfil,EstadoUI,verificacion2fa/*}, Aprendiz/{RegistrarEquipo,EditarPerfil}) — P0 de contraste (badges estado, botones contentColor, chips, ternarios, ErrorBox, focus de inputs) resueltos; botones primarios de sub-pantallas a píldora 28dp; doc-blocks `/** */` convertidos a `//` en todo el código de UI (incluye utilidades ios/SpringSpecs, PressScale, etc.); fix del recorte de foto en `EditarPerfil.kt` (color del anillo capturado en variable composable para usarlo en DrawScope). QA Fase 3: 12 P0 + P1 focus/shapes verificados OK. BUILD SUCCESSFUL, APK 22,018,089 bytes PENDIENTE instalar.
- 2026-09-13 — v3.1 verde original + fichas por ambiente + login alineado + QA global aprobado: `SenaGreen` y `SenaGreenOnLight` vuelven al tono original `0xFF02D914` (idéntico en ambos temas, decisión del dueño; comentario de `verdeMarca()` actualizado y `Theme.kt` secondary del verde viejo `00E676` a `02D914`); `AmbienteDetalleView.kt` agrupa estudiantes por ficha (`groupBy user_coursenumber`, chips FlowRow clicables, ficha seleccionada persiste tras recargar); `LoginScreen.kt` email pasa a `AuthField` + `menuAnchor()` (misma alineación que la contraseña) e imports muertos quitados; borrados los últimos 4 archivos del módulo jornada muerto (`jornada/`). QA global front+back: 5 botones admin con texto blanco (1.9:1) sobre verde en oscuro → `contentColor = Color.Black` (píldoras) en ActualizarUsuario/CrearUsuario/ReporteNovedades. BUILD SUCCESSFUL 22,090,098/4,341,631 bytes; release 3.0 instalado en moto g86 (ZY32MFMZMX). Pendiente opcional: push de código (sin keystore.properties), copiar APK a `public/SenaAccess.apk` del WEB y depurar 2FA sobre Railway si el QA lo marca.
- 2026-09-14 — Rediseño total v4 DS + seguridad + QA: nuevo `ui/ds/` (`SenaTokens`, `SenaRoutes/dockKeyFor`, `SenaComponents`: TopBar/Scaffold/Card/Badge/Search/Chip/Dialog); `glassSurface` sin +4dp, `GlowSpheres` 0.28 en claro, headers/topbar/dropdown a 16/24dp, `Presentes` sin mocks + `HorizontalDivider`, `Usuarios` a `LazyColumn` con key + tarjeta fullWidth + botones 48dp; seguridad: huella `invalidatedByEnrollment=true`, backup excluye stores, sin permisos muertos/autofill, `FLAG_SECURE`, pass min 8 en registro/crear/editar, `RolSeguro` en equipos, 2FA con 5 intentos/30s + timeout 10min. BUILD SUCCESSFUL, APK pendiente instalar.
- 2026-09-14 — Fase 2 DS adoptado + QA: shells a `SenaSpacing/dockKeyFor`, `TableContainer` responsivo sin 600dp, `Usuarios` sin Lazy anidado + `SenaSearchField`, `AdminEquipos/Presentes` a `SenaSearchField/SenaCell`, badge a `SenaBadge`, rutas `AprendizDest/InstructorDest/AdminTab`; QA fase 2 aprobado sin P0. BUILD SUCCESSFUL 22,213,049 bytes, instalado en moto g86 (ZY32MFMZMX) tras desinstalar release 3.0 por firma distinta.
- 2026-09-14 — v3.0 instalación limpia + push + QR: nuevo `data/VersionGuard.kt` (al cambiar versionCode purga correos/dispositivo/huella/sesión/retos, la app arranca desde cero sin heredar rol de la versión anterior); APK debug v3.0 (code 9, 22,229,751 bytes, md5 198777c3) a `public/SenaAccessV3.0.apk` del WEB + QR `~/Escritorio/QR-SenaAccess-v3.0.png`; push móvil (v1.8-merge) y WEB listos.
