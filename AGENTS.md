# AGENTS.md — Movilvistas ("SenaAccess MÓVIL")

App Android (Kotlin + Jetpack Compose, estilo glass verde) de SennAccess. Ruta: `/home/andres/Escritorio/VistasAndroidDef1-main./VistasAndroidDef1-main` (la carpeta padre termina en punto; citar siempre entre comillas).
No es repo git local; el código está en `git@github.com:Andres777233/VistasAndroidDef1.git` (rama `main`). Para push: clonar → superponer código → commitear → pushear.

## Comandos y entorno
- Compilar: `./gradlew assembleDebug` → APK en `app/build/outputs/apk/debug/app-debug.apk`.
- Instalar (moto g86): `/home/andres/android-sdk/platform-tools/adb install -r app/build/outputs/apk/debug/app-debug.apk`. El `adb reverse tcp:8000` lo aplica solo el watcher (≤3 s tras conectar USB).
- Backend: servicio systemd de usuario `sennaccess` (`systemctl --user status|restart sennaccess`); el watcher lo relanza si muere.
- Android Studio Flatpak: si el sync falla con "initialization script ... does not exist", ejecutar `./gradlew --stop` antes.
- Credenciales (BD Railway + local, ver seeder del WEB): `admin@sena.edu.co` con `12345678`; instructores: `juan.pablo@sena.edu.co` (`12345678`), `alejandro/gustavo/raul@sena.edu.co` (`123456`); aprendices: `andres.vargas/laura.medina@sena.edu.co` (`12345678`), `katherin/sebastian/camilo@sena.edu.co` (`123456`). NO existen `instructor@` ni `aprendiz@sena.edu.co`.

## Convenciones
- API: `RetrofitClient.conServicio{}` habla SOLO con Railway (HTTPS, sin fallback local); timeout conexión 10 s / lectura 60 s.
- Patrón `CargaUiState` (Loading/Success/Error); mocks SOLO en `data/mock/MockData.kt` y solo si `SessionManager.token == null`.
- Paquetes: dashboards en `com.example.sennaccess.Aprendiz` (comparten StatCard/TableContainer), ViewModels en `ui`, repos en `data`.
- Comentarios nuevos solo `//` en línea propia (sin asteriscos/emojis/cajas); cabecera 2-4 líneas `//` por archivo.
- Biometría LOCAL (`data/HuellaCredentialStore.kt`, Keystore AES256-GCM); el WebAuthn del backend quedó sin uso desde el móvil.

## Retomar trabajo (IMPORTANTE)
- Este proyecto = **"SenaAccess MÓVIL"**.
- Palabra clave: **"continuemos con Sena access movil"** (o "continuemos"/"retomemos"/"seguimos"). En ese caso NO leas archivos extra y NO re-explores el repo con grep/glob — el estado completo y todo el historial están AQUÍ MISMO (sección `## Historial`, entradas más recientes al final).

## Autoguardar historial (OBLIGATORIO)
Al terminar cada tarea/sesión, agrega al FINAL de `## Historial` una entrada fechada `- YYYY-MM-DD — <qué cambiaste y por qué>`: MUY concreta, directa y clara (1-2 líneas máximo). Nunca borres ni reescribas entradas anteriores.

## Historial
- 2026-08-22 — Paridad con el backend + refresco: quité de la app REPORTES/ASIGNACIONES/MENSAJE, arreglé dock EQUIPOS del admin (abre inventario); refetch automático al cambiar/volver de pestaña. APK compilado PENDIENTE de instalar en el moto g86.
- 2026-08-22 — USB automático permanente: servicio systemd user `sennaccess` + watcher cada 3 s (levanta artisan serve y aplica adb reverse solo con dispositivo autorizado); enable-linger activo; `iniciar-servidor.sh` quedó solo-estado.
- 2026-08-22 — Avatar/foto perfil: `ui/AvatarPerfil.kt` (Coil, rutas relativas vía `SessionManager.fotoUrl()`), CAMBIAR FOTO multipart (`updateMyProfileWithPhoto`, _method=PUT+image); admin solo muestra avatar.
- 2026-08-20 — Biometría LOCAL (reemplaza WebAuthn/passkeys): huella descifra credenciales del dispositivo (Keystore AES256-GCM, SharedPreferences `huella_store`); auto-sanitiza si el backend rechaza; WebAuthn queda sin uso desde el móvil.
- 2026-08-20 — FIX crash splash: themes.xml pasó a padre `Theme.AppCompat.Light.NoActionBar` (MainActivity extiende AppCompatActivity por BiometricPrompt).
- 2026-08-12 — Todos los GETs conectados a la API real (patrón CargaUiState, mocks solo en MockData.kt); logout best-effort crea Salida; historiales con badge ENTRADA/SALIDA; RegistrarEquipoView con accesorios dinámicos.
- 2026-08-12 — Push a GitHub VistasAndroidDef1 (estrategia clonar→superponer→push único "VersionBackend"); convención comentarios solo `//` línea propia.
- 2026-08-11 — Base técnica: Retrofit doble ruta USB/WiFi con fallback 3s; dashboards comparten StatCard/TableContainer; navbar/menú vidrio verde; RegisterScreen con diálogo "Solicitud enviada"; fix DirectoryLock de Android Studio Flatpak.
- 2026-08-24 — Preparado para Railway: RetrofitClient añade BASE_URL_REMOTE (HTTPS) como 3er fallback USB->WiFi->Railway; backend WEB con railway.json (nixpacks + migrate --seed) y .gitignore que protege .env. Ambos repos SUBIDOS a GitHub (SenaAccessWeb / SenaAccessMovil). Falta: deploy Railway + poner dominio real en BASE_URL_REMOTE y recompilar APK.
- 2026-08-24 — Fix build Railway (WEB): nixpacks.toml (PHP 8.3 + ext gmp/sodium/intl/bcmath + COMPOSER_ALLOW_SUPERUSER) para el fallo de package:discover como root; .gitkeep en storage/bootstrap (carpetas viajan en el repo, contenido ignorado); releaseCommand con mkdir/chmod + migrate --seed. Empujado a SenaAccessWeb (7d03601).
- 2026-08-24 — Fix "valid cache path": composer.json añade pre-autoload-dump con mkdir -p de storage/bootstrap antes de package:discover; probado localmente borrando views/ y corriendo dump-autoload (exit 0). Dominio Railway OK (senaaccessweb-production.up.railway.app); BASE_URL_REMOTE actualizado y APK compilado PENDIENTE instalar en moto g86 (no estaba conectado).
- 2026-08-24 — APK instalado en moto g86 y login probado contra Railway: funciona. Distribución QR: repo QR-MOVIL actualizado (SenaAccess.apk apuntando a Railway, página v1.0) + QR en ~/Escritorio/QR-SenaAccess-v1.0.png.
- 2026-08-24 — FIX login 500 con PC apagada: la BD "MySQL" de Railway estaba rota (solo placeholders, password root perdida, sin DATABASE_URL en el web). Creé BD nueva "MySQL-DK1Y" (railway add), inyecté en SenaAccessWeb DATABASE_URL+APP_KEY+APP_ENV+APP_DEBUG+APP_URL+LOG_CHANNEL=stderr, migré+sembré vía `railway ssh` al contenedor web; login verificado 200 contra Railway. RetrofitClient ahora Railway primario (REMOTE->USB->WIFI); APK compilado PENDIENTE instalar.
- 2026-08-25 — Pantalla Asignaciones (aprendiz↔instructor): Repository + ViewModel + AsignacionesView (listar/crear/eliminar con dropdowns de aprendiz/instructor/jornada); dock admin ahora tiene 6 tabs (se añadió "Asignar"). Verificación contra Railway: POST/GET/DELETE `/admin/aprendiz-instructores` funciona; equipo-a-usuario y novedades ya estaban cableados y verificados. APK compilado PENDIENTE instalar.
- 2026-08-25 — Quité la feature Asignaciones (aprendiz↔instructor) a petición del usuario (también se quitó del WEB): borré AsignacionesView/AsignacionRepository, revertí el dock a 5 tabs, limpié ViewModel/Mock/ApiService/Models. Además FIX: la pestaña EQUIPOS del admin estaba vacía porque `irATab("EQUIPOS")` no abría subScreen; ahora abre `AdminScreen.EQUIPOS` y el admin puede registrar/ver equipos con dueño (instructor/aprendiz). APK compilado PENDIENTE instalar (celular no conectado).
- 2026-08-25 — Perfil con foto en los 3 roles + UI más llamativa: nuevos `ui/PerfilUI.kt` (PerfilHeader con avatar 116dp anillo/glow + chip rol, FilaDato con ícono); `EditarPerfilView` gana `mostrarFichaPrograma` para el admin (oculta ficha/programa); ApiService/UsuarioRepository hacen opcionales ficha/programa en el multipart para que el admin suba foto; AdminDashboard añade sub-pantalla EDITAR_PERFIL. PerfilAprendizView/PerfilInstructorView/PerfilContent usan el nuevo diseño. APK compilado PENDIENTE instalar (celular no conectado).
- 2026-08-25 — FIX foto perfil (500) + ficha solo para Aprendiz: en el backend WEB `user_coursenumber`/`user_program` pasan a `required` SOLO para Aprendiz (updateMyProfile/createUser/updateUser) y Railway recibió CLOUDINARY_URL/CLOUDINARY_UPLOAD_PRESET (sin esto subir foto daba 500). Móvil: `EditarPerfilView` con `mostrarFichaPrograma=false` también para el instructor (no envía ficha/programa), `UpdateProfileRequest` con ficha/programa nullable, catch de error muestra código+detalle del servidor, y las vistas de perfil de admin/instructor muestran ficha solo si >0. Deploy Railway 34c08d55 verificado (login 200). APK compilado PENDIENTE instalar (celular se desconectó).
- 2026-08-25 — FIX foto no se guarda/error al cambiarla: `EditarPerfil.kt` ahora comprime la imagen elegida con `comprimirImagen()` (muestreo + escala a máx 1024px, JPEG calidad 80) y siempre sube como image/jpeg; evita el 422 por fotos >2MB o HEIC. CAUSA RAÍZ en Railway: la migración nullable de ficha/programa estaba PENDING ahí y el UPDATE con ficha null daba 500 tras subir bien a Cloudinary; aplicada vía `railway ssh` (columnas Null=YES) y subida verificada end-to-end contra Railway (HTTP 200, URL Cloudinary persistida). Backend también subió límite a max:5120 (push 6c137c0). APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-25 — Red y errores (decisión: SOLO Railway): `RetrofitClient` sin fallback USB/WiFi (era la causa probable del "Error 500" del dashboard: el celular caía al backend local roto), timeouts 10s/60s; `detalleHttp()` en CargaUiState/EditarPerfil muestra código+mensaje real del servidor (adiós al genérico "No se pudo conectar"); `comprimirImagen` con takePersistableUriPermission. APK instalado en moto g86; pendiente confirmar en el dispositivo que dashboard carga sin 500 y la foto guarda.
- 2026-08-25 — FIX "No se pudo leer la imagen": el `?: return null` de comprimirImagen se disparaba SIEMPRE porque decodeStream con inJustDecodeBounds devuelve null por diseño. Reescrita: ImageDecoder (API28+, ALLOCATOR_SOFTWARE, escala a 1024px) + BitmapFactory antigua + fallback bytes crudos; mensaje local ya no dice "Fallo de conexión". APK instalado en moto g86; pendiente prueba real del usuario.
- 2026-08-25 — Recortador circular de foto de perfil (sin librerías): en EditarPerfil.kt nuevo `RecortarFotoDialog` a pantalla completa (marco 300dp + anillo verde, gestos pinza-zoom/arrastre con límites para que el círculo siempre quede cubierto); al confirmar recorta con Matrix a Bitmap cuadrado 512px JPEG q85 (`bitmapAJpeg`) que se sube por multipart. Flujo: galería → recortar → previsualiza avatar → guardar; `comprimirImagen` eliminada (reemplazada por `decodificarImagen` máx 2048px). APK compilado e instalado en moto g86 (ZY32MFMZMX).
- 2026-08-25 — QR v1.1: repo QR-MOVIL actualizado (push 74aaed6 vía SSH; el clon local vive en /tmp) con el APK nuevo y página "v1.1 — actualizada 25/08/2026"; despliegue verificado en andres777233.github.io/QR-MOVIL (APK sirve 200). Nuevo QR en ~/Escritorio/QR-SenaAccess-v1.1.png (misma URL, verificada decodificándola).
- 2026-08-26 — Pulido v1.1: fechas legibles (ui/Formatos.kt: "25 ago 2026, 09:30", relativo "hace 5 min" en notis/novedades, hora corta en admin), pull-to-refresh en los 3 dashboards (PullToRefreshBox), estados vacíos amables (EstadoVacio en ui/EstadoUI.kt), pantalla Acerca de/versión (ui/AcercaDeView.kt, BuildConfig habilitado, versionName 1.1) accesible desde el menú de los 3 roles, y diálogo de confirmación real al borrar usuarios. APK compilado y QR-MOVIL actualizado (push 563f2bd, página v1.1 26/08/2026); pendiente instalar en moto g86.
- 2026-08-26 — Registro funcional + tipo de documento y teléfono: backend WEB añade columnas `user_documento_tipo` (CC/CE/TI/PAS) y `user_telefono` (migración + fillable + register/createUser/updateUser) y valida que el correo termine en @gmail.com, @hotmail.com o @soy.sena.edu.co (422 con mensaje claro si no). ROL resuelto: el registro público queda SOLO para aprendices (activos al instante, se corrigió el mensaje falso de "un admin validará tu cuenta"); los instructores SOLO los crea el admin desde su panel (CrearUsuario). Móvil: RegisterScreen con chips de tipo doc + teléfono y diálogo de éxito honesto; CrearUsuario/ActualizarUsuario con los campos nuevos; Models (RegisterRequest/UserRequest/User/UsuarioApi). WEB React: Register.jsx y Admin.jsx con select tipo doc + teléfono. Verificado contra Railway (gmail 201, soy.sena 201, outlook 422, login admin/instructor 200). Deploy e57dbb69→03030bc. APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-26 — Pulido registro: el tipo de documento pasa de chips a dropdown con significado ("CC: Cédula de Ciudadanía", "CE: Cédula de Extranjería", "TI: Tarjeta de Identidad", "PAS: Pasaporte") en RegisterScreen/CrearUsuario/ActualizarUsuario (mismo formato en Register.jsx/Admin.jsx) y el label del correo queda "Correo Electrónico" (sin "Institucional"). APK instalado en moto g86 (ZY32MFMZMX); WEB push f261336.
- 2026-08-26 — Flujo invitado COMPLETO (QR + escáner): backend WEB `registerGuest` ahora acepta `user_lastname` (push 289a345, auto-deploy Railway). Móvil: GuestForm (LoginScreen) con documento+nombre+apellidos llama `POST /register-guest`, valida con `detalleHttp` y muestra el QR de un solo uso (60 min) en `ui/QRUtils.kt` (genera Bitmap con zxing:core 3.5.3). Escáner en recepción: `admin/EscanearQrView.kt` (CameraX 1.4.2 + MLKit barcode) valida `POST /validate-guest-qr`; accesible desde menú de admin (AdminScreen.ESCANEAR_QR) e instructor (top bar). Verificado end-to-end contra Railway (guest con apellido 201, validación 1er uso OK / 2do uso "QR ya utilizado", entrada "Validación QR" en historial del admin). APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-26 — Control de permanencia "quién está dentro": backend WEB nuevo `GET /admin/presentes` (AdminController, push 68fdf5b): usuarios cuyo último ingreso es Entrada sin Salida (subquery MAX(id_ingreso)). Móvil: `Presente` en Models + `getPresentes` en ApiService/IngresoRepository, estado `presentes` en AdminDashboardViewModel, nueva `admin/Presentes.kt` (total + tarjetas con hora de entrada `horaCorta` y estancia `fechaRelativa`), pestaña 6ª "PRESENTES" en el dock del admin (ícono Groups). FCM (push) diferido. Verificado contra Railway (lista actual). APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-29 — Dashboard Aprendiz/Instructor enriquecido (sin quitar pestañas): `ResumenView` y `InstructorResumenView` ahora con banner bienvenida (avatar+ficha/programa), estado DENTRO/FUERA, actividad reciente (3) y equipos recientes (2) con "Ver todo" → dock, e instructor además novedades recientes (2) + acción Escanear QR; `TarjetaNovedad` pública para reuso; nuevos helpers `BienvenidaDashboardCard`/`SeccionDashboardTitulo`/`EstadoAccesoDashboardCard`/`TarjetaActividadDashboard`/`TarjetaEquipoDashboard` en `AprendizDashboard.kt:581`. BUILD SUCCESSFUL, APK en `app/build/outputs/apk/debug/app-debug.apk` pendiente instalar.
- 2026-08-29 — Limpieza dashboard (a petición): quitadas StatCards "Mis Ingresos/Equipos" y "Usuarios/Ingresos" y sección "TUS EQUIPOS" de `ResumenView` e `InstructorResumenView` (`AprendizDashboard.kt:327`, `InstructorDashboard.kt:349`); el Inicio queda solo con banner bienvenida + estado DENTRO/FUERA + actividad reciente (instructor además novedades + botón Escanear QR). BUILD SUCCESSFUL (44M), `adb install -r` OK en moto g86 (ZY32MFMZMX).
- 2026-08-29 — Autocompletado de correos + menú foto/lápiz: `SessionManager.kt:58` añade `guardarCorreoUsado`/`obtenerCorreosUsados` (SharedPreferences `login_prefs`, máx 5, sin duplicados); `LoginScreen.kt:92` guarda el correo al `LoginUiState.Success` y `NormalLoginForm:585` muestra `ExposedDropdownMenuBox` con `sugerenciasCorreo` filtradas (`menuAnchor`); `AprendizDashboard.kt:209` y `InstructorDashboard.kt:213` añaden `onEditarPerfil` y el header del menú pasa de `Icon(Person)` a `AvatarPerfil(fotoPath=SessionManager.userPhoto, 48.dp)` con el lápiz como botón circular verde (22dp) `clickable { onEditarPerfil ?: onPerfil }` → `EDITAR_PERFIL`. BUILD SUCCESSFUL, APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-29 — FIX modo claro (contenedores blancos debajo de texto): causa `ThemeText` blanco fijo sobre vidrio blanco → texto invisible; corregido `LoginScreen:518/590/858`, `RegisterScreen:449`, `PasswordReset:138/314`, `PasswordRecovery:165/384`, `QRUtils:91` a `colors.textPrimary/Secondary`; `GlassComponents:120` ahora distingue claro/oscuro (luminance) con base 0.92, highlight 0.55/0.18, sombra suave y borde definido; `Color:120` paleta clara más contrastada (bg 0xF0F2F5, tablas blancas/filas alternas, bordes 0.13); `GlassDock:75` sombra/borde adaptativos; `MainActivity:59` y `SplashScreen:100` usan `appColors.background`. BUILD SUCCESSFUL, APK instalado en moto g86 (ZY32MFMZMX).
- 2026-08-29 — Pulido splash + limpieza instructor: `SplashScreen.kt:81` acorta delays (650→450, 320→280, glow 500→420), glow más intenso en claro (0.72/0.28 vs 0.55/0.18) y añade `v${BuildConfig.VERSION_NAME}` con `LocalAppColors.textSecondary`; `InstructorDashboard.kt:37/52` quita imports `EscanearQrView`/`TarjetaNovedad`, `InstructorTopBar:213` elimina `onEscanearQr` y el DropdownMenuItem "Escanear QR invitado", `InstructorResumenView:358` elimina params `novedadesEstado/onVerNovedades/onEscanearQr` y borra secciones "MIS NOVEDADES" (L447) y "Control de invitados/ESCANEAR QR" (L478). BUILD SUCCESSFUL, APK instalado en moto g86 (ZY32MFMZMX).
- 2026-09-01 — Módulo presencia física + FSM jornada (v1.2): desacopla login de registro de presencia; 3 capas zero-trust — TOTP QR rotativo 30s (instructor proyecta, `jornada/GenerarQrAulaView.kt` + `TotpUtils.kt` liviano HMAC-SHA1), GPS con anti-spoofing (`UbicacionProveedor.kt` + `isMock`/`isFromMockProvider`, sin Play Services) y BSSID (`WifiProveedor.kt`); FSM `REGISTRADO→EN_AULA⇄EN_DESCANSO→FINALIZADO` con salida anticipada bloqueada (403) y token de permiso auditable (`JornadaViewModel.kt` + `AutorizarSalidaView.kt`); nuevo `AprendizDock` tab Jornada (`MarcarPresenciaView.kt`), instructor con QR Aula+Permisos en dock y admin en menú; contrato `ApiService.kt:259`/`ModelsJornada.kt` (NTP, ventanas por ambiente, cron 18:00). BUILD SUCCESSFUL v1.2 (44M), pendiente instalar.
- 2026-09-02 — Módulo ambientes + el QR de invitado eliminado (v1.3): eliminado `LoginScreen GuestForm`/`QRUtils QrInvitadoView`/`EscanearQrView`/rutas `register-guest`/`validate-guest-qr` (WEB `AuthController`+`api.php`); backend migraciones `ambientes`+`ambiente_instructor`+`ambiente_aprendiz` (hora_inicio/fin, totp_secret auto, capacidad/ubicación/jornada), modelo `Ambiente.php` y `AmbienteController.php`+`JornadaController.php` (TOTP 30s, `mis-ambientes`); móvil `AmbienteRepository.kt`/`AmbientesAdminView.kt` (admin crea ambientes multi-instructor, CCyS/Ciudad Jardín) + `MisAmbientesView/AmbienteDetalleView` (instructor ve sus salones, agrega/quita aprendices, accesos directos QR/Permiso); `InstructorDashboard` dock reestructurado 5 tabs. BUILD SUCCESSFUL v1.3 (44M) instalado en moto g86; verificado local: admin crea ambiente 101/202, instructor Juan Pablo en 2 ambientes, QR `SENA-JORNADA:1:283330:30` válido.
- 2026-09-02 — Fix 408 ambientes + excusas con PIN (v1.4): causa 408 era deploy desactualizado (Railway en 68fdf5b sin modulo); commit 25d10f9 deploy + railway ssh migrate crea ambientes; verificado POST/PUT/GET/DELETE /admin/ambientes y /mis-ambientes/{id}/aprendices contra Railway (101 CCyS y 202 Ciudad Jardin con Juan Pablo). Excusas: backend migracion excusas + Excusa.php/ExcusaController.php con PIN 4 digitos (60 min, un solo uso) — instructor POST /instructor/excusas -> PIN, admin POST /excusas/validar registra Salida en ingresos; movil ModelsExcusa.kt/ExcusaRepository.kt + excusas/CrearExcusaView.kt (instructor elige ambiente/aprendiz->PIN copiable) + excusas/ValidarExcusaView.kt (admin valida) + excusas/MisExcusasView.kt (aprendiz) integrados en InstructorDashboard (AMBIENTES->CrearExcusa), AdminDashboard (menu Validar excusa), AprendizDashboard (dock EXCUSAS). Verificado Railway: PIN 1232 usado, 5416/1992 pendientes. BUILD SUCCESSFUL v1.4 (45M) pendiente instalar (moto g86 no conectado).
- 2026-09-02 — Limpieza v1.4: quitada tab JORNADA del aprendiz (`AprendizDashboard.kt:77` quita `JornadaViewModel`/`MarcarPresenciaView` y `GlassDockItem JORNADA`; dock queda Inicio/Excusas/Historial/Equipos) y menú "Autorizar salida" del admin (`AdminLayout.kt:204`); el flujo real de salida es Validar excusa PIN (admin) y Crear excusa PIN (instructor). BUILD SUCCESSFUL, APK reinstalado en moto g86 (ZY32MFMZMX) y QR-MOVIL v1.4 actualizado (push ea1721a, 45M).
